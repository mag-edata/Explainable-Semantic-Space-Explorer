# テスト項目書 — Explainable Semantic Space Explorer

**プロジェクト名:** Explainable Semantic Space Explorer  
**版:** 1.0  
**作成日:** 2026-05-05

**凡例**

| 記号 | 意味 |
|------|------|
| 優先度: H / M / L | 高 / 中 / 低 |
| 種別: UT / ST | 単体テスト / システムテスト |
| 結果: O / X / △ / - | 合格 / 不合格 / 条件付き合格 / 未実施 |

---

## 1. DistanceMetrics テストケース

**対象ファイル:** `tests/test_distance_metrics.py` → `core/distance_metrics.py`

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| DM-01 | `l2_norm([3.0, 4.0])` が `5.0` を返す（ピタゴラスの定理） | H | UT | O |
| DM-02 | `l2_norm([1.0, 0.0, 0.0])` が `1.0` を返す（単位ベクトル） | H | UT | O |
| DM-03 | `l2_norm(np.zeros(4))` が `0.0` を返す（ゼロベクトル） | H | UT | O |
| DM-04 | `l2_norm([1, 1, 1, 1])` が `2.0`（= √4）を返す | M | UT | O |
| DM-05 | `l2_norm` の戻り値型が Python `float`（numpy スカラーではない） | M | UT | O |
| DM-06 | `l2_norm` に Python list を渡すと `TypeError` が送出される | M | UT | O |
| DM-07 | `l2_norm` に shape (2, 2) の2次元配列を渡すと `VectorDimensionError` が送出される | H | UT | O |
| DM-08 | 300次元乱数ベクトルに対して `l2_norm` が `sqrt(sum(v²))` と一致する（places=4） | H | UT | O |
| DM-09 | `cosine_similarity([1,2,3], [2,4,6])` が `1.0` を返す（平行ベクトル） | H | UT | O |
| DM-10 | `cosine_similarity([1,2,3], [-1,-2,-3])` が `-1.0` を返す（逆方向ベクトル） | H | UT | O |
| DM-11 | `cosine_similarity([1,0], [0,1])` が `0.0` を返す（直交ベクトル） | H | UT | O |
| DM-12 | 片方がゼロベクトルのとき `cosine_similarity` が `0.0` を返す（NaN 化しない） | H | UT | O |
| DM-13 | 50次元乱数20ペアに対して全 `cosine_similarity` 結果が `[-1.0, 1.0]` 範囲に収まる | H | UT | O |
| DM-14 | `cosine_similarity(a, b) == cosine_similarity(b, a)` が成立する（対称性） | H | UT | O |
| DM-15 | 次元数が異なるベクトルを渡すと `VectorDimensionError` が送出される | H | UT | O |
| DM-16 | `cosine_similarity_batch(query(3,), matrix(4,3))` の出力 shape が `(4,)` になる | H | UT | O |
| DM-17 | バッチの行0がクエリと同一のとき `result[0] == 1.0` | H | UT | O |
| DM-18 | バッチの行1が直交のとき `result[1] == 0.0` | H | UT | O |
| DM-19 | バッチの行2が逆方向のとき `result[2] == -1.0` | H | UT | O |
| DM-20 | バッチの行3が45度のとき `result[3] == 1/√2` | M | UT | O |
| DM-21 | ゼロ行を含む行列でも NaN が発生せず、ゼロ行の結果が `0.0` になる | H | UT | O |
| DM-22 | `cosine_similarity_batch` の結果が単一計算と一致する（places=4、10行バッチ） | H | UT | O |
| DM-23 | クエリと行列の次元が不一致のとき `VectorDimensionError` が送出される | H | UT | O |
| DM-24 | `explain` の返却辞書に `dot_product` `norm_a` `norm_b` `denominator` `similarity` `formula` の6キーが存在する | M | UT | O |
| DM-25 | `explain` の `formula` フィールドが `str` 型である | M | UT | O |
| DM-26 | `explain` を直交ベクトルで呼ぶと返却辞書の `similarity == 0.0` | H | UT | O |
| DM-27 | `explain` を平行ベクトルで呼ぶと返却辞書の `similarity == 1.0` | H | UT | O |
| DM-28 | `explain` の `dot_product` が `np.dot(a, b)` と一致する | H | UT | O |
| DM-29 | `explain` の `norm_a` および `norm_b` が `>= 0.0` である（ノルム非負性） | M | UT | O |

---

## 2. EmbeddingLoader テストケース

**対象ファイル:** `tests/test_embedding_loader.py` → `core/embedding_loader.py`

テストでは `_build_mock_assets()` ヘルパで小規模モック資産（n=5、static_dim=4、contextual_dim=8）を `tempfile.TemporaryDirectory` に動的生成する。

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| EL-01 | `load_all()` 後の `static_vectors.shape == (5, 4)`（n=5, dim=4 モック） | H | UT | O |
| EL-02 | `load_all()` 後の `contextual_vectors.shape == (5, 8)`（n=5, contextual_dim=8 モック） | H | UT | O |
| EL-03 | `load_all()` 後の `vocab` が `dict` 型である（リスト形式から変換される） | H | UT | O |
| EL-04 | `load_all()` 後の `len(vocab) == 5`（n=5 モック） | H | UT | O |
| EL-05 | `load_all()` 後の `pos.shape == (5,)` | H | UT | O |
| EL-06 | `load_all()` 後の `manifest` が dict であり `"static_vectors"` キーを持つ | M | UT | O |
| EL-07 | `vocab` のインデックス値集合が `set(range(N))` に一致する（0 起点連続インデックス） | H | UT | O |
| EL-08 | `static_vectors.dtype == np.float32` | H | UT | O |
| EL-09 | `manifest` の static_shape を `(99, 4)` に上書きすると `ManifestViolationError` が送出される | H | UT | O |
| EL-10 | `manifest` の contextual_shape を `(5, 999)` に上書きすると `ManifestViolationError` が送出される | H | UT | O |
| EL-11 | `manifest` の dtype を `"float64"` に上書き（実体は float32）すると `ManifestViolationError` が送出される | H | UT | O |
| EL-12 | vocab=5件に対して `vocab_pos.npy` を3件にすると `IndexAlignmentError` が送出される（N 不一致） | H | UT | O |
| EL-13 | vocab インデックスに飛びがある場合（`{"word0":0,"word1":1,"word2":9}`）`IndexAlignmentError` が送出される | H | UT | O |
| EL-14 | vocab インデックスが重複する場合（`{"word0":0,"word1":0}`）`IndexAlignmentError` が送出される | H | UT | O |
| EL-15 | 存在しないパスで `EmbeddingLoader` を生成すると `FileNotFoundError` が送出される | H | UT | O |
| EL-16 | `manifest.json` が欠落した状態で `load_all()` を呼ぶと `FileNotFoundError` が送出される | H | UT | O |
| EL-17 | `static_vectors.npy` が欠落した状態で `load_all()` を呼ぶと `FileNotFoundError` が送出される | H | UT | O |

---

## 3. SimilarityEngine テストケース

**対象ファイル:** `tests/test_similarity_engine.py` → `core/similarity_engine.py`

テストでは `_make_engine()` ヘルパで n=6（または 10）、dim=4 の小規模エンジンを `numpy.random.default_rng(seed)` で生成する。

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| SE-01 | `search("word0", top_k=3)` の戻り値が `list[SearchResult]` である | H | UT | O |
| SE-02 | `search("word0", top_k=3)` の返却件数が `<= 3` | H | UT | O |
| SE-03 | `search` の結果にクエリ語自身（`"word0"`）が含まれない（自己除外） | H | UT | O |
| SE-04 | `search` の返却リストが `similarity` 降順に並んでいる | H | UT | O |
| SE-05 | `search` 結果の `results[0].rank == 1`（rank が 1 始まり） | H | UT | O |
| SE-06 | `search` 結果の `explanation` に `"formula"` `"dot_product"` キーが存在する | M | UT | O |
| SE-07 | `search("word0", pos_filter="NOUN")` で全結果の `pos_tag == "NOUN"` | M | UT | O |
| SE-08 | `search("nonexistent_word")` で `UnknownWordError` が送出される | H | UT | O |
| SE-09 | `search("word0", top_k=0)` で `InvalidTopKError` が送出される | H | UT | O |
| SE-10 | `search("word0", top_k=-1)` で `InvalidTopKError` が送出される | H | UT | O |
| SE-11 | `compare` の戻り値が `ComparisonResult` である | M | UT | O |
| SE-12 | `compare` 結果の `query_word` フィールドがクエリ語と一致する | M | UT | O |
| SE-13 | `compare` 結果の `static_results` と `contextual_results` の件数がともに `<= top_k` | M | UT | O |
| SE-14 | `compare` 結果の `common_words` が `static_words ∩ contextual_words` と等しい | M | UT | O |
| SE-15 | `compare` 結果の `rank_diff` のキー集合が `common_words` と一致する | M | UT | O |
| SE-16 | `compare("no_such_word", ...)` で `UnknownWordError` が送出される | H | UT | O |
| SE-17 | `get_distance_distribution("word0")` の戻り値が `dict` である | M | UT | O |
| SE-18 | 返却辞書に `query_word` `mean` `std` `top1_similarity` `z_score` `histogram_data` の6キーが存在する | H | UT | O |
| SE-19 | 返却辞書の `query_word` フィールドがクエリ語と一致する | M | UT | O |
| SE-20 | `top1_similarity >= mean - 1e-6`（Top-1 が平均以上） | H | UT | O |
| SE-21 | `len(histogram_data) == n - 1`（クエリ自身を除く件数、n=10） | H | UT | O |
| SE-22 | `std >= 0.0`（標準偏差が非負） | H | UT | O |
| SE-23 | `get_distance_distribution("no_such_word")` で `UnknownWordError` が送出される | H | UT | O |
| SE-24 | `word_to_index` が vocab 内の全単語に対して正しいインデックスを返す | H | UT | O |
| SE-25 | `word_to_index("unknown_word_xyz")` で `UnknownWordError` が送出される | H | UT | O |

---

## 4. POSFilter テストケース

**対象ファイル:** `tests/test_pos_filter.py` → `core/pos_filter.py`

テストでは NOUN×3 / VERB×2 / ADJ×1 の合計 6 件の `SearchResult` を直接構築する `_sample_results()` を使用する。

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| PF-01 | `filter("NOUN")` が NOUN タグの SearchResult のみを返す | H | UT | O |
| PF-02 | `filter` の戻り値が元の results の順序を保つ | M | UT | O |
| PF-03 | `filter` の戻り値で元の `rank`（全体順位）が保たれる | M | UT | O |
| PF-04 | `filter("")` で `ValueError` が送出される | M | UT | O |
| PF-05 | results に存在しない品詞を指定すると `UnknownPOSTagError` が送出される | H | UT | O |
| PF-06 | `group_by_pos` の戻り値が `dict` 型である | M | UT | O |
| PF-07 | `group_by_pos` のキー集合が results に登場する全品詞と一致する | H | UT | O |
| PF-08 | 各品詞グループの件数が正しい（NOUN=3 / VERB=2 / ADJ=1） | H | UT | O |
| PF-09 | グループ内順序が元の results の順序を保つ | M | UT | O |
| PF-10 | 空 results に対して `group_by_pos` が空 dict を返す | M | UT | O |
| PF-11 | `pos_distribution` で各品詞の件数が正しく集計される | H | UT | O |
| PF-12 | `pos_distribution` が件数の多い順にソートされている | H | UT | O |
| PF-13 | `pos_distribution` の合計が results の長さと一致する | M | UT | O |
| PF-14 | 空 results に対して `pos_distribution` が空 dict を返す | M | UT | O |
| PF-15 | `heterogeneity_rate(query_pos="NOUN")` が 3/6=0.5 を返す | H | UT | O |
| PF-16 | `heterogeneity_rate(query_pos="VERB")` が 4/6 を返す | H | UT | O |
| PF-17 | 全件が同品詞のとき `heterogeneity_rate` が 0.0 を返す | H | UT | O |
| PF-18 | 全件が異品詞のとき `heterogeneity_rate` が 1.0 を返す | H | UT | O |
| PF-19 | `heterogeneity_rate` の戻り値が `[0.0, 1.0]` の範囲に収まる | M | UT | O |
| PF-20 | 空 results で `heterogeneity_rate` を呼ぶと `ValueError` が送出される | H | UT | O |
| PF-21 | `query_pos=""` で `heterogeneity_rate` を呼ぶと `ValueError` が送出される | M | UT | O |
| PF-22 | `assign_pos_ranks` 後、各品詞グループの先頭 `pos_rank` が 1 から始まる | H | UT | O |
| PF-23 | `assign_pos_ranks` 後、同品詞内 `pos_rank` が 1, 2, 3 ... と連続する | H | UT | O |
| PF-24 | `assign_pos_ranks` が元の results の順序を破壊しない | M | UT | O |
| PF-25 | `assign_pos_ranks` の戻り値の件数が入力と一致する | M | UT | O |
| PF-26 | `top_pos()`（n=3）で出現上位 3 品詞が降順で返る | M | UT | O |
| PF-27 | `top_pos(n=1)` で最頻品詞のみが返る | M | UT | O |
| PF-28 | n が品詞種数を超えても利用可能な全品詞を超えない | M | UT | O |
| PF-29 | `top_pos(n=0)` で `ValueError` が送出される | M | UT | O |

---

## 5. Analyzer テストケース

**対象ファイル:** `tests/test_analyzer.py` → `core/analyzer.py`

テストでは `_make_distribution()` で `get_distance_distribution()` の戻り値を模した dict を、`_make_search_result()` で `SearchResult` を構築する。

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| AN-01 | `enrich_distribution` の戻り値が `DistributionStats` である | H | UT | O |
| AN-02 | `query_word` が引き継がれる | M | UT | O |
| AN-03 | `median` フィールドが `numpy.median` と一致する | H | UT | O |
| AN-04 | `q25 <= median <= q75` の関係が成立する | H | UT | O |
| AN-05 | `n_samples` が `histogram_data` の長さと一致する | M | UT | O |
| AN-06 | distribution に必須キーが欠けていると `KeyError` が送出される | H | UT | O |
| AN-07 | 空 `histogram_data` で `InsufficientDataError` が送出される | H | UT | O |
| AN-08 | `histogram` の戻り値が `HistogramData` である | M | UT | O |
| AN-09 | `bin_edges` の長さが `n_bins + 1` である | H | UT | O |
| AN-10 | `counts` の長さが `n_bins` と一致する | H | UT | O |
| AN-11 | `counts` の合計が data の件数と一致する | H | UT | O |
| AN-12 | `data_min` / `data_max` が data の最小・最大と一致する | M | UT | O |
| AN-13 | 空 data で `histogram` を呼ぶと `InsufficientDataError` が送出される | H | UT | O |
| AN-14 | `n_bins=0` で `histogram` を呼ぶと `ValueError` が送出される | M | UT | O |
| AN-15 | `attach_z_scores` の戻り値が `dict` のリストである | M | UT | O |
| AN-16 | `attach_z_scores` の戻り値の件数が results と一致する | H | UT | O |
| AN-17 | 各要素に必須キー（word/rank/similarity/pos_tag/pos_rank/z_score/explanation）が含まれる | H | UT | O |
| AN-18 | `z_score` が `(similarity - mean) / std` と一致する | H | UT | O |
| AN-19 | `std=0` のとき `z_score=0.0` が安全に返る（ゼロ除算ガード） | H | UT | O |
| AN-20 | `compare_distributions` の戻り値が `DistributionComparison` である | M | UT | O |
| AN-21 | `compare_distributions` の `query_word` が引き継がれる | M | UT | O |
| AN-22 | `mean_diff = static.mean - contextual.mean` と一致する | H | UT | O |
| AN-23 | `std_diff = static.std - contextual.std` と一致する | H | UT | O |
| AN-24 | `z_score_diff = static.z_score - contextual.z_score` と一致する | H | UT | O |
| AN-25 | `static_stats` / `contextual_stats` が `DistributionStats` 型として格納される | M | UT | O |
| AN-26 | `neighborhood_stability` が完全一致時に 1.0 を返す | H | UT | O |
| AN-27 | `neighborhood_stability` が完全不一致時に 0.0 を返す | H | UT | O |
| AN-28 | 部分一致時に正しい Jaccard 係数を返す（共通=2, 和=4 → 0.5） | H | UT | O |
| AN-29 | `neighborhood_stability` の戻り値が `[0.0, 1.0]` の範囲に収まる | M | UT | O |
| AN-30 | 空の `static_results` で `neighborhood_stability` を呼ぶと `ValueError` が送出される | H | UT | O |
| AN-31 | 空の `contextual_results` で `neighborhood_stability` を呼ぶと `ValueError` が送出される | H | UT | O |

---

## 6. KMeansClusterer テストケース

**対象ファイル:** `tests/test_clustering.py` → `transforms/clustering.py`

テストでは `_random_vectors()` 乱数生成と、3 中心の `_separable_vectors()` を使う。

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| CL-01 | デフォルト引数でインスタンス化できる | M | UT | O |
| CL-02 | カスタム引数（n_clusters=5, seed=7, max_iter=100）でインスタンス化できる | M | UT | O |
| CL-03 | `n_clusters=0` で `InvalidClusterCountError` が送出される | H | UT | O |
| CL-04 | `n_clusters=-1` で `InvalidClusterCountError` が送出される | H | UT | O |
| CL-05 | `n_clusters` が int でない場合 `TypeError` が送出される | M | UT | O |
| CL-06 | `seed` が int でない場合 `TypeError` が送出される | M | UT | O |
| CL-07 | `fit` の戻り値が `ClusterResult` である | H | UT | O |
| CL-08 | `labels` の shape が `(N,)` である | H | UT | O |
| CL-09 | `labels` の値が `[0, n_clusters - 1]` の範囲に収まる | H | UT | O |
| CL-10 | `n_samples` フィールドが入力件数と一致する | M | UT | O |
| CL-11 | `n_clusters` フィールドが指定値と一致する | M | UT | O |
| CL-12 | `seed` フィールドが指定値と一致する | M | UT | O |
| CL-13 | `inertia` が非負である | H | UT | O |
| CL-14 | 明確に分離されたデータで近接点が同一クラスタに属する | H | UT | O |
| CL-15 | `np.ndarray` 以外で `UnfitVectorError` が送出される | H | UT | O |
| CL-16 | 1 次元配列を渡すと `UnfitVectorError` が送出される | H | UT | O |
| CL-17 | `n_clusters > N` で `InvalidClusterCountError` が送出される | H | UT | O |
| CL-18 | `fit` 後に `get_labels` でラベルが取得できる | H | UT | O |
| CL-19 | `fit` 前の `get_labels` で `NotFittedError` が送出される | H | UT | O |
| CL-20 | `fit` 後に `get_result` で `ClusterResult` が取得できる | H | UT | O |
| CL-21 | `fit` 前の `get_result` で `NotFittedError` が送出される | H | UT | O |
| CL-22 | `_l2_norm_batch([[3,4],[0,0],[1,0]])` が `[5,0,1]` を返す | H | UT | O |
| CL-23 | `_l2_norm_batch` の出力 shape が `(N,)` である | M | UT | O |
| CL-24 | `_l2_norm_batch` のノルムが非負である | M | UT | O |
| CL-25 | `_normalize_rows` 後、ゼロでない行のノルムが 1.0 になる | H | UT | O |
| CL-26 | ゼロベクトル行は正規化後もゼロのまま（NaN にならない） | H | UT | O |
| CL-27 | `_normalize_rows` が入力 matrix を破壊しない | M | UT | O |
| CL-28 | 同一 seed・同一データで `labels` が完全一致する（再現性） | H | UT | O |
| CL-29 | 同一 seed で `inertia` も完全一致する（再現性） | H | UT | O |

---

## 7. Projector テストケース

**対象ファイル:** `tests/test_projection.py` → `transforms/projection.py`

テストでは `_random_vectors(n=20, dim=8)` を使用。UMAP は計算コストを抑えるため小規模に限定する。

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| PR-01 | デフォルト手法が `pca` である | M | UT | O |
| PR-02 | `method='pca'` でインスタンス化できる | M | UT | O |
| PR-03 | `method='umap'` でインスタンス化できる | M | UT | O |
| PR-04 | サポート外の手法名（"tsne" 等）で `InvalidMethodError` が送出される | H | UT | O |
| PR-05 | `method` が str でない場合 `TypeError` が送出される | M | UT | O |
| PR-06 | `seed` が int でない場合 `TypeError` が送出される | M | UT | O |
| PR-07 | PCA の戻り値が `ProjectionResult` である | H | UT | O |
| PR-08 | PCA の `coords_2d` の shape が `(N, 2)` である | H | UT | O |
| PR-09 | PCA の `method` フィールドが "pca" である | M | UT | O |
| PR-10 | PCA の `explained_variance` の要素数が 2 である | H | UT | O |
| PR-11 | PCA の各寄与率が `[0.0, 1.0]` の範囲に収まる | H | UT | O |
| PR-12 | PCA の寄与率の合計が 1.0 以下である | H | UT | O |
| PR-13 | 第 1 主成分の寄与率が第 2 主成分以上である | H | UT | O |
| PR-14 | PCA の `n_samples` が入力件数と一致する | M | UT | O |
| PR-15 | PCA の `seed` フィールドが指定値と一致する | M | UT | O |
| PR-16 | `fit_transform` 直後の `cluster_labels` が None である | M | UT | O |
| PR-17 | UMAP の `coords_2d` の shape が `(N, 2)` である | H | UT | O |
| PR-18 | UMAP の `method` フィールドが "umap" である | M | UT | O |
| PR-19 | UMAP の `explained_variance` が空リストである | H | UT | O |
| PR-20 | `np.ndarray` 以外で `InvalidVectorError` が送出される | H | UT | O |
| PR-21 | 1 次元配列で `InvalidVectorError` が送出される | H | UT | O |
| PR-22 | サンプル数 1 で `InvalidVectorError` が送出される | H | UT | O |
| PR-23 | `attach_clusters` の戻り値が `ProjectionResult` である | M | UT | O |
| PR-24 | `attach_clusters` で `cluster_labels` が指定したラベル配列と一致する | H | UT | O |
| PR-25 | `attach_clusters` が元の result を破壊しない（イミュータブル） | H | UT | O |
| PR-26 | `attach_clusters` の戻り値が元の result と別オブジェクトである | M | UT | O |
| PR-27 | `attach_clusters` で `coords_2d` / `explained_variance` / `method` が引き継がれる | M | UT | O |
| PR-28 | ラベル長が `n_samples` と一致しない場合に `InvalidVectorError` が送出される | H | UT | O |
| PR-29 | result が `ProjectionResult` でない場合に `TypeError` が送出される | M | UT | O |
| PR-30 | `cluster_labels` が `np.ndarray` でない場合に `TypeError` が送出される | M | UT | O |
| PR-31 | PCA の同一 seed で `coords_2d` が完全一致する（再現性） | H | UT | O |
| PR-32 | UMAP の同一 seed で `coords_2d` が完全一致する（再現性） | H | UT | O |

---

## 8. UI テストケース

**対象ファイル:** `ui/app.py`（ブラウザによる手動確認）

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| A-01 | Streamlit アプリが起動し、タイトルと4タブが表示される | H | ST | O |
| A-02 | 検索タブでクエリ語を入力・実行すると Word2Vec / SBERT の Top-K 類似語が2カラム表示される | H | ST | O |
| A-03 | 各結果行に類似度スコア・内積・ノルム（コサイン内訳）・品詞ラベルが表示される | H | ST | O |
| A-04 | 比較タブで共通語数・固有語数・差分ランキングが表示される | M | ST | O |
| A-05 | 投影タブで PCA / UMAP の選択に応じた 2D 散布図が表示される | M | ST | O |
| A-06 | クラスタタブでクラスタ色分け散布図と代表語リストが表示される | M | ST | O |
| A-07 | 検索実行中にローディングスピナーが表示される | M | ST | O |

---

## 9. エラーケース テストケース

**対象:** エラーメッセージ E001–E006（`st.error()` による赤字表示、手動確認）

| No. | テスト概要 | 優先度 | 種別 | 結果 |
|-----|-----------|--------|------|------|
| X-01 | 空文字で検索実行すると E001「クエリ語を入力してください。」が表示される | H | ST | O |
| X-02 | 語彙外の単語を入力すると E002「〜は語彙に含まれていません。」が表示される | H | ST | O |
| X-03 | `data/` の `.npy` / `.json` が見つからない起動時に E003 が表示される | H | ST | - |
| X-04 | `manifest.json` と実ファイルの shape / dtype が不一致のとき E004 が表示される | H | ST | - |
| X-05 | Top-K に 0 以下の値を指定すると E005「Top-K は 1 以上の整数を指定してください。」が表示される | M | ST | O |
| X-06 | クラスタ数に 1 以下の値を指定すると E006「クラスタ数は 2 以上の整数を指定してください。」が表示される | M | ST | O |

---

## テスト実施サマリー

| 項目 | 詳細 |
|------|------|
| 実施日 | 2026-05-06（UT 拡充）、2026-05-05（ST 手動） |
| 実施者 | mag |
| テスト総数 | 205 件（UT: 192 件、ST: 13 件） |
| 合格（O） | 203 件（DM-01〜29、EL-01〜17、SE-01〜25、PF-01〜29、AN-01〜31、CL-01〜29、PR-01〜32、A-01〜07、X-01、X-02、X-05、X-06） |
| 未実施（-） | 2 件（X-03、X-04） |
| 不合格（X） | 0 件 |

**備考**

- UT 71 件（§1〜§3）はすべて `venv/bin/python3 -m unittest discover tests/ -v` で実行・全件 PASS 済み。
- ST（§4・§5）はブラウザでの手動確認。Streamlit セッション状態の自動テストは未整備のため目視確認とする。
- X-03 / X-04 は `data/` を意図的に欠落・改ざんする操作が必要なため、本番資産配置後に手動で実施する。
