# =============================================================================
# Explainable Semantic Space Explorer — .gitignore
# =============================================================================


# -----------------------------------------------------------------------------
# Python 実行環境
# 仮想環境ディレクトリはサイズが大きく、環境依存のため除外。
# 再構築は: python3 -m venv venv && pip install -r requirements.txt
# -----------------------------------------------------------------------------
venv/
.venv/
env/
__pycache__/
*.py[cod]        # .pyc / .pyo / .pyd をまとめて除外
*.pyd
.Python
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/


# -----------------------------------------------------------------------------
# 大容量データ資産（assets/）
# 埋め込みベクトルは数百 MB に達するため Git 管理外とする。
# 別途 DVC や LFS での管理を推奨。
# -----------------------------------------------------------------------------
assets/embeddings/*.npy
assets/metadata/vocab_pos.npy
assets/metadata/vocab.json


# -----------------------------------------------------------------------------
# manifest.json は意図的にトラッキングする（shape/dtype の仕様書として機能）
# → assets/manifest.json は除外しない
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# 環境変数・シークレット
# APIキーや認証情報を誤ってコミットしないための保護。
# -----------------------------------------------------------------------------
.env
.env.*
*.secret
secrets.json
secrets.yaml


# -----------------------------------------------------------------------------
# Streamlit キャッシュ・設定（ui/ 実装後に生成される）
# .streamlit/secrets.toml には認証情報が入ることがあるため除外。
# .streamlit/config.toml は共有したい場合は除外リストから外すこと。
# -----------------------------------------------------------------------------
.streamlit/secrets.toml
.streamlit/cache/
__pycache__/


# -----------------------------------------------------------------------------
# テスト・カバレッジ（tests/ 実装後に生成される）
# -----------------------------------------------------------------------------
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.cover
.hypothesis/


# -----------------------------------------------------------------------------
# Jupyter Notebook（分析・デバッグ用として将来追加される可能性あり）
# -----------------------------------------------------------------------------
.ipynb_checkpoints/
*.ipynb


# -----------------------------------------------------------------------------
# ログファイル（logging モジュールが出力するファイルを想定）
# -----------------------------------------------------------------------------
*.log
logs/


# -----------------------------------------------------------------------------
# OS 固有ファイル
# -----------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini


# -----------------------------------------------------------------------------
# エディタ・IDE 設定
# プロジェクト固有の設定は .vscode/settings.json 等で管理する場合は
# 該当行をコメントアウトして共有すること。
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~


# -----------------------------------------------------------------------------
# UMAP / scikit-learn のキャッシュ（analysis/ 実装後に生成される可能性あり）
# joblib の Memory キャッシュディレクトリ。
# -----------------------------------------------------------------------------
joblib_cache/
__joblib_memory__/


# -----------------------------------------------------------------------------
# ドキュメント生成（将来 Sphinx 等を使う場合）
# -----------------------------------------------------------------------------
docs/_build/
docs/api/


# =============================================================================
# 除外しないもの（明示）
# =============================================================================
# CLAUDE.md          → プロジェクト設定ファイル（コミット対象）
# requirements.txt   → 依存関係定義（コミット対象）
# assets/manifest.json → shape/dtype の仕様書（コミット対象）
# README.md          → プロジェクト説明（コミット対象）
