# =============================================================================
# Explainable Semantic Space Explorer — .gitignore
# =============================================================================


# -----------------------------------------------------------------------------
# Python runtime
# Virtual environment directories are large and environment-dependent,
# so they are excluded from Git tracking.
# -----------------------------------------------------------------------------
venv/
.venv/
env/
.env
.env.*
__pycache__/
*.py[cod]
.Python
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/


# -----------------------------------------------------------------------------
# Large data assets (data/)
# Embedding vectors can reach several hundred MB, so they are excluded
# from Git tracking. Manage them via DVC or Git LFS separately.
# -----------------------------------------------------------------------------
data/embeddings/*.npy
data/metadata/vocab_pos.npy
data/metadata/vocab.json
data/nltk_data/


# -----------------------------------------------------------------------------
# Word2Vec model files (models/)
# The model itself, along with its companion syn1neg / wv.vectors files,
# reaches several hundred MB and is excluded from Git tracking.
# -----------------------------------------------------------------------------
models/*.model
models/*.model.syn1neg.npy
models/*.model.wv.vectors.npy


# -----------------------------------------------------------------------------
# manifest.json is intentionally tracked and remains under Git control.
# → (serves as the shape/dtype specification)
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# Environment variables / secrets
# Guards against accidentally committing API keys or credentials.
# -----------------------------------------------------------------------------
.env
.env.*
*.secret
secrets.json
secrets.yaml


# -----------------------------------------------------------------------------
# Streamlit cache / settings (generated after ui/ is implemented)
# Excluded because .streamlit/secrets.toml may contain credentials.
# Remove .streamlit/config.toml from the exclude list if you want to share it.
# -----------------------------------------------------------------------------
.streamlit/secrets.toml
.streamlit/config.toml
.streamlit/cache/


# -----------------------------------------------------------------------------
# Tests / coverage (generated after tests/ is implemented)
# -----------------------------------------------------------------------------
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.cover
.hypothesis/


# -----------------------------------------------------------------------------
# Jupyter notebooks (may be added later for analysis / debugging)
# -----------------------------------------------------------------------------
.ipynb_checkpoints/
*.ipynb


# -----------------------------------------------------------------------------
# Log files (expected to be emitted by the logging module)
# -----------------------------------------------------------------------------
*.log
logs/


# -----------------------------------------------------------------------------
# OS-specific files
# -----------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini


# -----------------------------------------------------------------------------
# Editor / IDE settings
# If project-specific settings are managed in .vscode/settings.json or similar,
# comment out the corresponding line to share them.
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~


# -----------------------------------------------------------------------------
# UMAP / scikit-learn cache (may be generated after transforms/ is implemented)
# joblib's Memory cache directory.
# -----------------------------------------------------------------------------
joblib_cache/
__joblib_memory__/


# -----------------------------------------------------------------------------
# Documentation generation (in case Sphinx or similar is adopted in the future)
# -----------------------------------------------------------------------------
DOCS/_build/
DOCS/api/


# -----------------------------------------------------------------------------
# Claude Code
# -----------------------------------------------------------------------------
.claude/
# CLAUDE.md


# -----------------------------------------------------------------------------
# Explicitly NOT excluded (tracked on purpose)
# -----------------------------------------------------------------------------
# data/manifest.json → shape/dtype specification (tracked)
